import { MasonaryDirective } from './masonary.directive';

describe('MasonaryDirective', () => {
  it('should create an instance', () => {
    const directive = new MasonaryDirective();
    expect(directive).toBeTruthy();
  });
});
